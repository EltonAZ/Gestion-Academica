package com.colegio.colegio.entity;

// Enumeración que define los roles de usuario dentro del sistema
public enum Rol {
    ADMIN,          // Usuario con privilegios de administrador
    DOCENTE,        // Usuario con rol de docente
    ESTUDIANTE      // Usuario con rol de estudiante
}

// Es un ENUM que Se usa para gestionar permisos y roles dentro del sistema.
// Enum (enumeración): Es un tipo especial en Java que define un conjunto fijo de valores constantes.